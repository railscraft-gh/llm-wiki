제시해주신 소스 문서(`raw/API_Key_관리_및_Infisical_도입_가이드.md`)를 바탕으로 작성 원칙과 스키마 구조를 엄격히 준수하여 **`시크릿 관리자`** 위키 노트를 새로 작성하였습니다.

생성된 파일 경로: [시크릿 관리자.md](file:///Users/railscraft/.gemini/antigravity-cli/scratch/llm-wiki/wiki/%EC%8B%9C%ED%81%AC%EB%A6%BF%20%EA%B4%80%EB%A6%AC%EC%9E%90.md)

---

### 주요 반영 사항

1. **Frontmatter 스키마 준수**:
   - `type`: `tool`
   - `status`: `draft`
   - `core`: `false`
   - `tags`: `['llm', 'agent', 'security', 'secret-management', 'devops']`
   - `sources`: `['raw/API_Key_관리_및_Infisical_도입_가이드.md']`
   - `created` 및 `updated`: `2026-08-28`
2. **검증 가능한 출처 표기**:
   - 12-Factor App 원칙, 로테이션 주기(30~90일, 90~180일), 700+ 유출 패턴 검증, K8s Base64 인코딩 한계 등 핵심 지표와 수치 문단 끝마다 소스 문서 경로 `(raw/API_Key_관리_및_Infisical_도입_가이드.md)`를 명시했습니다.
3. **구체적 코드 및 시나리오 3종 포함**:
   - **Infisical CLI**: `.env` 파일 없이 런타임에 임시 환경 변수를 주입하여 Node.js/Python 앱을 실행하는 워크플로우.
   - **Node.js SDK**: Universal Auth(Machine Identity)를 통해 시크릿 관리자 서버로부터 동적으로 DB URL을 조회하는 예시.
   - **Kubernetes Operator**: External Secrets Operator 방식으로 Pod 매니페스트 변경 없이 K8s Secret을 자동 생성·동기화하는 매니페스트.
4. **Obsidian 위키링크 연동**:
   - [[Infisical]], [[AI 게이트웨이]], [[인증 및 권한 부여]], [[Kubernetes 시크릿 오퍼레이터]] 등 관련 지식 개념 간 위키링크를 자연스럽게 포함했습니다.