요청하신 **'가이드 디코딩'**에 관한 고품질 위키 노트를 작성하여 성공적으로 지식 베이스에 등록했습니다.

노트는 `/Users/railscraft/.gemini/antigravity-cli/scratch/llm-wiki/wiki/가이드 디코딩.md`에 저장되었으며, 주요 작성 내용은 아래와 같습니다.

---

### [작성된 위키 노트 요약]

- **문서 위치**: [가이드 디코딩.md](file:///Users/railscraft/.gemini/antigravity-cli/scratch/llm-wiki/wiki/%EA%B0%80%EC%9D%B4%EB%93%9C%20%EB%94%94%EC%BD%94%EB%94%A9.md)
- **Frontmatter**: 지정된 스키마 규격(`type: concept`, `status: draft`, `sources`, `created`, `updated` 등) 완벽 준수
- **한 줄 정의**: LLM의 추론(Generation) 단계에서 JSON 스키마, 정규식 등의 구조적 제약을 강제 주입하여 사전에 정의된 정형 규격(Structured Output)에 완벽히 부합하는 토큰만 디코딩 단계부터 생성하도록 제어하는 기술
- **핵심 요지 & 상세**:
  - vLLM 서빙 엔진의 `extra_body={"guided_json": schema}`를 활용한 Grammar-Constrained Decoding 방식 해설
  - 7B 소형 경량 모델(Qwen2.5-Coder-7B-Instruct)의 서술적 수사 이탈 및 문법 깨짐 현상을 물리적으로 차단하는 신뢰성 레이어(Reliability Layer) 역할 설명
  - 가이드 디코딩 실패 시 프롬프트 주입 및 중괄호 깊이 추적 파서(`extract_first_json`)를 구동하는 구조적 예외 복구(Fallback) 메커니즘 도식화 및 수록
  - 모델의 성급한 심각도(Severity) 부풀리기를 방지하기 위해 도달 가능성(`reachability`), 전제 조건(`preconditions`) 등을 필수 항목으로 강제하는 스키마 기법 정리
- **구체적 예시**:
  - Python 기반 vLLM `chat_json` 및 `extract_first_json` 파서 모듈 코드 예시 수록
  - 심각도(Severity) 평가용 `SEVERITY_SCHEMA` 코드 예시 수록
- **관련 위키 노트 연동**: `[[검색 및 수정 루프]]`, `[[에이전트 하네스 엔지니어링]]`, `[[에이전트 제어 루프]]` 등 기존 LLM Wiki 노트와 자연스럽게 연결