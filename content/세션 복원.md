요청해주신 **'세션 복원'** 개념 노트 작성이 지정된 템플릿 규칙 및 작성 가이드라인에 맞춰 완료되었습니다.

### 📝 작성된 노트 개요
- **파일 경로**: `scratch/llm-wiki/wiki/세션 복원.md`
- **Frontmatter 구성**:
  - `type`: `workflow`
  - `status`: `draft`
  - `core`: `false`
  - `tags`: `['llm', 'agent', 'websocket', 'voice-agent', 'gemini-live']`
  - `sources`: `['raw/Build a Real-Time Voice Agent in 30 Minutes (With Interruption Handling)-ko.md']`
  - `created` / `updated`: `2026-08-28`
- **주요 내용 요약**:
  1. **한 줄 정의**: 네트워크 끊김이나 웹소켓 타임아웃 발생 시 전체 대화 내역 재전송 없이 캐싱된 핸들 식별자로 스트리밍 세션을 재개하는 워크플로우 메커니즘.
  2. **핵심 요지 및 검증 지표**: 오디오 Live 채널 제한(15분), 웹소켓 커넥션 서버 `GoAway` 경고 신호 주기(약 10분), `SessionResumptionUpdate` 응답 처리, 토큰 생명주기(세션 파괴 후 2시간 유효) 등 소스 문서의 지표 명시 (`raw/Build a Real-Time Voice Agent in 30 Minutes (With Interruption Handling)-ko.md`).
  3. **실제 코드 예시**: `google-genai` SDK 및 `gemini-3.1-flash-live-preview` 모델 환경에서 `SessionResumptionConfig`와 `SessionResumptionUpdate`를 사용하여 재연결 루프를 구현한 Python 비동기 예시 제공.
  4. **위키 연동 및 백링크**: [[Gemini Live API]], [[네이티브 양방향 모델]], [[지터 버퍼]], [[직렬 연결형 파이프라인]] 등 관련 개념 노트와 상호 연결.