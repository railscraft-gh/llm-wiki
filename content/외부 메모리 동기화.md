요청하신 개념 **'외부 메모리 동기화'**(유형: `workflow`)에 대한 고품질 한국어 위키 노트를 작성하여 LLM Wiki 저장소에 저장 완료했습니다.

### 📄 작성된 위키 노트 파일
- **경로**: `/Users/railscraft/.gemini/antigravity-cli/scratch/llm-wiki/wiki/외부 메모리 동기화.md` (Obsidian 위키 링크: `[[외부 메모리 동기화]]`)

---

### 📝 위키 노트 요약 내용

1. **Frontmatter 스키마**:
   - `type: workflow`, `status: draft`, `core: false`
   - `tags: ['llm', 'agent', 'memory', 'harness-engineering', 'context-management']`
   - `sources: ['raw/99%의 사람보다 더 나은 Harness Engineer를 만드는 법.md']`
   - `created: '2026-08-27'`, `updated: '2026-08-27'`

2. **핵심 내용**:
   - **한 줄 정의**: AI 에이전트의 대화 휘발성 및 컨텍스트 윈도우 한계로 인한 세션 단절과 맥락 붕괴를 방지하기 위해, 주요 설계 결정·작업 진행 상황을 물리 파일(`progress.md` 등) 또는 외부 저장소에 기계적으로 동기화·복원하는 하네스(Harness) 메커니즘.
   - **라이프사이클**: `메모리 복원 Phase` → `태스크 실행 & 검증 Phase` → `메모리 동기화 갱신 Phase` → `세션 인계 Phase` (Mermaid 시퀀스 다이어그램 포함).
   - **Before vs After 비교**: 사용자 인증 API 구현 시나리오를 바탕으로 동기화 미적용 시 발생하는 품질 변동성/결정 무시 문제와 동기화 적용 시의 통제 효과를 비교 정리.

3. **구체적 코드 예시**:
   - Python 기반 `ExternalMemorySynchronizer` 클래스와 하네스 루프 연동 시나리오 구현 코드 포함 (`progress.md` 파일 읽기/쓰기/복원 기능).

4. **충돌 섹션**:
   - 수동 마크다운 파일 동기화(`progress.md`) vs 자동화된 DB/벡터 저장소 동기화 관점의 충돌.
   - 세션 단위 동기화 vs 도구/에피소드 단위 실시간 동기화 주기 간의 동시성 및 복구 타협점 정리.

5. **연관 노트 연동**:
   - [[하네스 엔지니어링]], [[에이전트 상태 관리]], [[AI 에이전트 제어 루프]], [[컨텍스트 부패]], [[지속성 장기 메모리]] 등 최소 5개 이상의 위키 노트와 상호 연결.