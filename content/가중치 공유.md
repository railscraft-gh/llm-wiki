요청해주신 소스 문서들을 바탕으로 개념 노트 **`가중치 공유`**를 작성하여 위키 디렉토리에 등록 완료했습니다.

생성된 파일 경로: [가중치 공유.md](file:///Users/railscraft/.gemini/antigravity-cli/scratch/llm-wiki/wiki/%EA%B0%80%EC%A4%91%EC%B9%98%20%EA%B3%B5%EC%9C%A0.md)

---

### 주요 반영 및 분석 내역

1. **frontmatter 스키마 준수**:
   - `type: concept`, `status: draft`, `core: false`, 지정 태그, 생성 및 수정 일자(`2026-08-28`)와 소스 문서 2건 목록 정밀 반영.
2. **개념의 다각도 정리**:
   - **순환 신경망(RNN)**: 시간 축(Timestep)을 따라 입력 데이터와 이전 은닉 상태 연산에 동일한 가중치($W_x, W_h$) 및 편향($b$)을 재사용하는 기법과 그로 인한 BPTT 기울기 소실/폭주 영향 서술 (`raw/Understanding Transformers (Part 1). Why RNNs are nearly impossible to train.md`).
   - **트랜스포머/GPT (Weight Tying)**: 토큰 ID를 벡터로 변환하는 입력 임베딩 행렬(`wte`)을 최종 어휘 사전 점수를 계산하는 프로젝션 행렬로 역재사용하는 파라미터 효율화 기법 정리 (`raw/Andrej Karpathy Just Built an Entire GPT in 243 Lines of Python.md`).
3. **구체적 예시 및 구체적인 수식/코드 제공**:
   - 안드레 카파시(Andrej Karpathy)의 243줄 파이썬 `microGPT`에서 `wte`를 `linear(x, state_dict['wte'])`로 재활용하는 파이썬 예시 코드 포함.
   - RNN 셀 연산에서 시간 단계별 공유 방정식 명시.
4. **연관 위키링크 연동**:
   - [[순환 신경망]], [[트랜스포머]], [[공개 가중치 모델]] 등의 Obsidian 위키링크를 자연스럽게 연동.

> **참고**: 작업 공간 설정 안내
> 현재 백엔드 위키 파일은 `/Users/railscraft/.gemini/antigravity-cli/scratch/llm-wiki` 디렉토리에 저장되어 있습니다. 향후 위키 작업 효율 향상을 위해 해당 디렉토리를 활성 작업 공간(Active Workspace)으로 지정하는 것을 권장해 드립니다.